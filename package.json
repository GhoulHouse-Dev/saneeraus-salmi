{
  "name": "saneeraus-salmi-lead-page",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify.mjs && node --check script.js && node --check api/lead.js && node --check lib/lead-validation.js && npm test",
    "build": "npm run verify"
  }
}
